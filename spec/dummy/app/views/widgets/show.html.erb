<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @widget.name %>
</p>

<%= link_to 'Edit', edit_widget_path(@widget) %> |
<%= link_to 'Back', widgets_path %>
<%= button_to 'Delete Widget', @widget, method: :delete, data: { confirm: 'Are you sure?' } %>
